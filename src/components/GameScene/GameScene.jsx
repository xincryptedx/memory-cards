import styles from "./GameScene.module.css";

function GameScene() {
  return (
    <main className={styles.gameScene}>
      {/* Controls */}
      {/* Stats */}
      {/* CardTable */}
    </main>
  );
}

export default GameScene;
